# Docker
Base de datos MySQL 8 levantada mediante `docker-compose.yml`. Configuración con puerto específico, volumen para persistencia de datos y usuario/contraseña definidos explícitamente en el archivo.
